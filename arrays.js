var friends = ["Alex", "Bryan", "Cindy", "David"];
var numbers = [2, 4, 6, 8];
var booleans = [true, false, true, false];

// This will call the friend at number 3 position which is the fourth friend David
console.log(friends[3]);

// This code will add numbers together
console.log(numbers[0]+numbers[1]);

// this code will add a true boolean to the beginning
booleans.unshift("true");
console.log(booleans)